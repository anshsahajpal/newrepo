apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name | default "task-management" }}
spec:
  selector:
    app: {{ .Values.service.name | default "task-management" }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port | default 9001 }}
      targetPort: {{ .Values.service.targetPort | default 9001 }}
  type: {{ .Values.service.type }}